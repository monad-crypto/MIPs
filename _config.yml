title: Monad Improvement Proposals
description: Monad Improvement Proposals (MIPs)
url: "https://mips.monad.xyz"
baseurl: 
lang: en
timezone: UTC

lsi: false
safe: true
incremental: false
theme: minima
markdown: kramdown
highlighter: none
permalink: /:path

plugins:
  - jekyll-redirect-from

# `safe: true` only loads whitelisted plugins, so the redirect plugin must be
# listed here as well as under `plugins` above.
whitelist:
  - jekyll-redirect-from

kramdown:
  input: GFM
  math_engine: mathjax
  syntax_highlighter: null

gist:
  noscript: false

defaults:
  -
    scope:
      path: "MIPs"
    values:
      layout: "mip"
  -
    scope:
      path: "MRCs"
    values:
      layout: "mrc"

exclude:
  - .github
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/

include:
  - LICENSE.md

sass:
  quiet_deps: true
  silence_deprecations:
    - import
